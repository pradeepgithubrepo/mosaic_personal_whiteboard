# Shared Modules - Infinite Whiteboard

This directory is reserved for models, types, and logic that will be shared between the frontend and backend in future phases (e.g. board JSON validation schemas, asset metadata, OCR payloads).
